
/**
 * Enum for the type of view to present during running of the algorithm.
 * These correspond to three different views in jMusic:
 * Notation, Score, and Sketch.
 * @author braem
 */
public enum ViewType {
	NOTATION, MIDISCORE, SKETCH;
}
